groups:
  - name: demo
    rules:
      - alert: InstanceDown
        expr: up == 0
        for: 1m
        labels: { severity: warning }
        annotations:
          summary: "Instance down"
          description: "{{ $labels.instance }} is down"

